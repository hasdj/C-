#include <stdio.h>
#include <add.h>
unsigned int a, b, c, d;
int add(int summ)
{
	unsigned int sum = 0;
	scanf_s("%d%d%d%d", &a, &b, &c, &d);
	sum = a + b + c + d;
	printf("20222703315ÁõÓî¶«\n");
	return sum;
}