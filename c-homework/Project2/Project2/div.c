#include<stdio.h>
#include<stdlib.h>
void  divide(int  dividend, int  divisor) //被除数，除数
{
	int remainder=0;//余数
	int temp = 0;
	while (dividend -divisor>=0)
		{
			dividend = dividend - divisor;
			remainder+=1;
		}
	
}

	